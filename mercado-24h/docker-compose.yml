version: "3.9"

services:
  app:
    build: .
    container_name: mercado-24h-flask
    environment:
      - FLASK_ENV=production
      - PORT=5000
      - PAYMENT_APPROVAL_RATE=0.7
    ports:
      - "5000:5000"
